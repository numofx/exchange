PRIVATE_KEY=

# Keyed RPC for the `celo` endpoint in foundry.toml (Celo mainnet, 42220).
# Referenced as ${CELO_RPC_URL} so the provider key stays out of git.
CELO_RPC_URL=

# Required for scripts/deploy-deliverable-fx-manager.s.sol
# Must point to an already deployed WrappedERC20Asset for deliverable USDC.
WRAPPED_USDC_DELIVERABLE_ASSET_ADDRESS=
